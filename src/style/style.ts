import {createTheme} from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#F8D198',
    },
    secondary: {
      main: '#9D355D',
    },
  },
});
export default theme;